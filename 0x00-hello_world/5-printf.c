#include <stdio.h>

/**
  * main - print to the stdout using printf
  * Return: 0 as success value status
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
